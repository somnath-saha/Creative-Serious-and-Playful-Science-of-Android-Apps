<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="abc.def.topmovies"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="abc.def.topmovies.MainActivity"
            android:configChanges="orientation|screenSize|uiMode"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="abc.def.topmovies.MainActivity2"
            android:configChanges="orientation|screenSize|uiMode"
            android:label="@string/title_activity_main_activity2" >
        </activity>
        <activity
            android:name="abc.def.topmovies.MainActivity3"
            android:configChanges="orientation|screenSize|uiMode"
            android:label="@string/title_activity_main_activity3" >
        </activity>
        <activity
            android:name="abc.def.topmovies.MainActivity4"
            android:configChanges="orientation|screenSize|uiMode"
            android:label="@string/title_activity_main_activity4" >
        </activity>
        <activity
            android:name="abc.def.topmovies.MainActivity5"
            android:configChanges="orientation|screenSize|uiMode"
            android:label="@string/title_activity_main_activity5" >
        </activity>
        <activity
            android:name="abc.def.topmovies.Start"
            android:label="@string/title_activity_start" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
